spring:
  application:
    name: hotel.booking
  h2:
    console:
      enabled: true
      path: /h2-console/
  datasource:
    url: jdbc:h2:mem:testdb
    username: sa
    password: sa
  jpa:
    hibernate:
      ddl-auto: update
logging:
  level:
    root: "info"
    org.springframework.web: "info"
    org.hibernate: "error"
server:
  port: 8116
  
  
   